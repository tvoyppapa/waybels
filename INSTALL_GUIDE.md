# 🚀 УСТАНОВКА AQUM С НУЛЯ

## ШАГ 1: УСТАНОВКА БАЗЫ ДАННЫХ

### Откройте в браузере:
```
http://ваш-домен/install_db.php
```

Этот скрипт:
- ✅ Удалит все старые базы данных
- ✅ Создаст новую БД `aqum_db`
- ✅ Создаст все таблицы
- ✅ Добавит тестовые данные

---

## ШАГ 2: ВХОД В СИСТЕМУ

После установки откройте:
```
http://ваш-домен/auth.php
```

### Тестовый аккаунт:
- **Email:** `test@test.com`
- **Пароль:** `password`

---

## ШАГ 3: ПРОВЕРКА

После входа вы должны попасть на:
```
http://ваш-домен/feed.php
```

---

## 🔧 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### 1. Проверьте `config.php`:
```php
define('DB_NAME', 'aqum_db');  // ← должно быть aqum_db
define('DASHBOARD_URL', 'feed.php');  // ← должно быть feed.php
```

### 2. Проверьте `db.php`:
```php
$dbname = "aqum_db";  // ← должно быть aqum_db
```

### 3. Проверьте права MySQL:
```bash
mysql -u root -p
# Пароль: WayBels2553030App!
```

---

## 📁 СТРУКТУРА БД

Таблицы:
- `users` - пользователи
- `teacher_profiles` - репетиторы
- `posts` - посты в ленте
- `post_likes` - лайки
- `favorites` - избранное
- `user_interactions` - взаимодействия

---

## ✅ ГОТОВО!

Теперь все должно работать!
